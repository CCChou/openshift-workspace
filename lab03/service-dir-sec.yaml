apiVersion: v1
kind: Service
metadata:
  name: config-test-dir
  labels:
    app: config-test-dir
spec:
  type: ClusterIP
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: config-test-dir